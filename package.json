{
  "name": "edupass-pick-beacon-test",
  "version": "0.0.1",
  "private": true,
  "description": "Prueba aislada de deteccion de balizas Bluetooth (BLE) para EduPass Pick. NO es parte de la app EduPass en produccion todavia -- solo sirve para validar el hardware antes de integrarlo.",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor-community/bluetooth-le": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
